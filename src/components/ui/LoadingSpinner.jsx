import React from 'react';

const LoadingSpinner = () => (
  <div style={{ textAlign: "center", padding: 20 }}>
    <span className="loader" style={{ fontSize: 24 }}>Loading...</span>
  </div>
);

export default LoadingSpinner;
